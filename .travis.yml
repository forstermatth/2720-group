language: cpp
compiler:
    - gcc
install:
    - sudo apt-get update
    - sudo apt-get install libcppunit-dev
    - mkdir bin
    - make
script: 
    - ./test
notifications:
    recipients:
        - forster.matth@gmail.com
    email:
        on_success: change
        on_failure: always
