language: cpp
compiler:
    - gcc
install:
    - sudo apt-get update
    - sudo apt-get install libcppunit-dev
    - mkdir bin
    - make
script: 
    - ./tests
notifications:
    recipients:
        - forster.matth@gmail.com
        - werrejustin@gmail.com
    email:
        on_success: change
        on_failure: always
